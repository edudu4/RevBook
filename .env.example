# Copie para .env (na raiz, ao lado de docker-compose.prod.yml) e preencha —
# usado só no deploy (EC2). Para desenvolvimento local, veja backend/.env.example.

GHCR_OWNER=

DB_USERNAME=revbook
DB_PASSWORD=
JWT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_BOOKS=
